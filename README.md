# git
git commands
git commands

git config --global user.email "your_address_email@example.com"
git config --global user.name "your_name"
git clone url -- how to take code in local machine 

git branch --- how to check all the branches

git branch branch_name  -- new branch 

git checkout branch_name -- go into yor branch 

git status -- check status what all changes we done 

git diff --ignore-all-space filename -- check the changes

git add .

git commit -m "" -- commit yor changes 

git push -u origin branch_name -- push your changes 

create pull request
